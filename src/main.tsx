import { render } from 'preact';
import { Routes } from './Routes.tsx'; // Asegúrate de que la ruta del archivo sea correcta
import './index.css';

render(<Routes />, document.getElementById('app')!);

